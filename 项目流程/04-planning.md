# 4. 实验规划

本次有效矩阵只有两个实际训练实验：

| 实验 | 步数 | batch | 目的 |
|---|---:|---:|---|
| CUDA 冒烟 | 200 | 8 | 验证 GPU、PyAV、离线模型和 checkpoint |
| 无增强基线 | 25,000 | 8 | 生成供本机筛选的原始 checkpoint |

不增加旧数据集变体，不启用图像增强，不改模型结构，不使用 `--resume=true`。
