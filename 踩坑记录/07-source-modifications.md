# 7. 源码、配置与可复现边界

训练复现优先使用命令行配置、明确的数据副本和完整环境报告，不把临时源码修改当作默认依赖。需要修改源码时，应记录：上游 commit、文件路径、最小 diff、动机、验证命令和是否已随交接包保存。

当前已知训练侧关键适配是图像 feature 映射，而不是必须修改 LeRobot 核心代码。`front` 与 `top` 的采集名称应在训练配置中显式映射到 `camera1`、`camera2`；混淆映射会导致训练读取错误视角，即使训练不报错也会污染结论。
