# 1. Ubuntu 系统与持久化路径

训练与结果均使用 `/data/coding`，不把关键产物留在 `/tmp` 或 home。初始可用空间约 59GB；用户扩容后数据盘为 159GB，环境安装后约 140GB 可用，训练和归档完成后约 105GB 可用。

离线包和最终实验目录分别为：

```text
/data/coding/so101-smolvla-training-bundle-2026-07-20
/data/coding/so101-smolvla-experiment-2026-07-20
```
